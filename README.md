# Games 

## Running Local

To run locally, follow these steps:
- `npm install`
- `npm run build`
- `npm run dev`

This will run the application on port 4000. 

## Tests

To run tests:
- `npm run test`

